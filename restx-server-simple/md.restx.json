{
    "parent": "io.restx:restx-parent:${restx.version}",
    "module": "io.restx:restx-server-simple:${restx.version}",

    "properties": {
        "@files": ["../restx.build.properties.json"]
    },

    "dependencies": {
        "compile": [
            "io.restx:restx-factory:${restx.version}",
            "io.restx:restx-core:${restx.version}",
            "org.simpleframework:simple:${simple.version}"
        ],
        "test": [
            "junit:junit:4.11",
            "org.mockito:mockito-all:1.9.5",
            "org.assertj:assertj-core:${assertj-core.version}",
            "com.github.kevinsawicki:http-request:${http-request.version}"
        ]
    }
}
