{
    "parent": "io.restx:restx-parent:${restx.version}",
    "module": "io.restx:restx-servlet:${restx.version}",

    "properties": {
        "@files": ["../restx.build.properties.json"]
    },

    "fragments": {
        "maven": [
            "classpath:///restx/build/fragments/maven/core-annotation-processing.xml"
        ]
    },

    "dependencies": {
        "compile": [
            "io.restx:restx-core:${restx.version}",
            "org.codehaus.janino:janino:${janino.version}"
        ],
        "provided": [
            "javax.servlet:servlet-api:${servlet-api.version}"
        ]
    }
}
