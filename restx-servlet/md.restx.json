{
    "parent": "fr.4sh.pom-parents:4sh-uberpom:0.8",
    "module": "restx:restx-servlet:${restx.version}",

    "properties": {
        "@files": ["../restx.build.properties.json"]
    },

    "dependencies": {
        "compile": [
            "restx:restx-core:${restx.version}",
            "javax.servlet:javax.servlet-api:${servlet-api.version}"
        ]
    }
}
